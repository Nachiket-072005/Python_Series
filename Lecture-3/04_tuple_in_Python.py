tup = (2, 1, 3, 1)
print(type(tup))
print(tup)
print(tup[0])
print(tup[1])

#Tuples are immutable in Python that means it will not allowed to change at particular index element using tup[0] = 4. Compiler gives error. as Strings also.